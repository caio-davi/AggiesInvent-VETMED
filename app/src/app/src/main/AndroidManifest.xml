<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tarecoapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TarecoApp">
        <activity android:name=".ScheduleActivity"></activity>
        <activity android:name=".GalleryActivity" />
        <activity android:name=".UrinaryActivity" />
        <activity android:name=".SkinEarsActivity" />
        <activity android:name=".MedicalActivity" />
        <activity android:name=".MedicationsActivity" />
        <activity android:name=".FoodActivity" />
        <activity android:name=".BathActivity" />
        <activity android:name=".HygieneActivity" />
        <activity android:name=".CareActivity" />
        <activity android:name=".PetProfileActivity" />
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".PetActivity">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>